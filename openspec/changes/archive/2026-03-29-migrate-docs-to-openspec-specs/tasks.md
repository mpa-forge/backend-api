## 1. Capture capability specs

- [x] 1.1 Create capability delta specs for the current API runtime, auth, database migration, and runtime-path documents
- [x] 1.2 Review the migrated requirements and scenarios to ensure they preserve the current documented behavior without introducing new scope

## 2. Preserve compatibility docs

- [x] 2.1 Update each existing `docs/` file to mark the OpenSpec capability as the canonical source and keep a short compatibility summary
- [x] 2.2 Keep existing repo documentation references working without requiring immediate path changes elsewhere in the repository

## 3. Finalize the migration

- [x] 3.1 Validate the OpenSpec change and fix any structural issues
- [x] 3.2 Archive the completed change so the canonical specs are written under `openspec/specs/`
