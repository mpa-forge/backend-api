## 1. Runtime

- [x] 1.1 Add local-only browser CORS handling for the protected Connect
  procedure paths in the API runtime.
- [x] 1.2 Allow the documented frontend SPA origins and reject unknown origins
  for local preflight requests.

## 2. Verification

- [x] 2.1 Add runtime tests for allowed and rejected browser preflight requests.
- [x] 2.2 Update runtime docs and the `api-runtime` OpenSpec capability to
  describe the local browser CORS contract.
- [x] 2.3 Run repo validation for the runtime change.
- [x] 2.4 Archive or sync the completed change after validation.
